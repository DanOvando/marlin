% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allocate_effort.R
\name{allocate_effort}
\alias{allocate_effort}
\title{Allocate fishing effort across patches}
\usage{
allocate_effort(
  effort_by_patch,
  total_effort_by_fleet = NULL,
  buffet,
  fleets,
  open_patch,
  clip_z = 5,
  scale = c("mad", "iqr", "sd"),
  eps_mix = 0,
  floor_frac = 0,
  flatness_tol = 0.001,
  min_scale_abs = 1e-10,
  adaptive_floor_pct = 0.01,
  objective_override = NULL
)
}
\arguments{
\item{effort_by_patch}{Numeric matrix with patches in rows and fleets in columns.}

\item{total_effort_by_fleet}{Optional numeric vector of total effort per fleet. If
\code{NULL}, totals are computed from \code{effort_by_patch}.}

\item{buffet}{Named list from \code{\link{go_fish}} with matrix outputs (for example
\code{r_p_fl}, \code{c_p_fl}, \code{prof_p_fl}, and per-unit variants).}

\item{fleets}{Named list of fleet objects (each with \code{$spatial_allocation}).}

\item{open_patch}{Logical matrix (patches x fleets) indicating where each fleet may fish.}

\item{clip_z}{Numeric scalar controlling how strongly extreme objective values affect the update.}

\item{scale}{Character; method used to scale the objective before updating effort.}

\item{eps_mix}{Numeric between 0 and 1; mix-in fraction for uniform exploration.}

\item{floor_frac}{Numeric; optional effort floor applied across open patches.}

\item{flatness_tol}{Numeric; threshold for treating the objective as flat across patches.}

\item{min_scale_abs}{Numeric; minimum scale to avoid division by near-zero values.}

\item{adaptive_floor_pct}{Numeric; fraction of the median objective used as an adaptive scale floor.}

\item{objective_override}{Optional numeric matrix (patches x fleets) supplying the per-fleet
objective surface directly. When supplied, it replaces the column extracted from
\code{buffet} for fleets whose \code{spatial_allocation} is one of the buffet-driven
strategies (\code{rpue}, \code{ppue}, \code{marginal_profit}, etc.). Used by
\code{\link{simmar}} to feed in a temporally-smoothed objective when a fleet has
\code{memory_halflife > 0}; \code{NULL} preserves current behavior.}
}
\value{
A named list containing the updated effort matrix (\code{effort_new}) and additional
diagnostic matrices/vectors used to track the update (for example standardised objectives
and flatness indicators).
}
\description{
Updates patch-level effort for one or more fleets based on an objective signal
contained in the \code{buffet} output from \code{\link{go_fish}}.
}
\details{
This helper is called by \code{\link{simmar}} each time step. For each fleet,
it converts the selected objective (e.g. revenue, catch, profit, or a per-unit
variant) into a smooth multiplicative update so that higher-objective patches
receive more effort, subject to \code{open_patch}.
}
\seealso{
\code{\link{go_fish}}, \code{\link{calc_marginal_value}}, \code{\link{simmar}}
}
